spring:
  datasource:
    url: jdbc:mysql://localhost:3306/seas_backend
    username: root
    password: goma
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database: mysql
  mail:
    host: smtp.gmail.com
    port: 465
    username: app.todolis@gmail.com
    password: wltpbbvqpguwmyqh
    properties:
      mail:
        smtp:
          auth: true
          ssl:
            enable: true
        starttls:
          enable: true
    protocol: smtp
server:
  port: 8088
